<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.task.dao.mapper.PermissionMapper">
  <resultMap id="BaseResultMap" type="com.task.entity.Permission">
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="code" jdbcType="VARCHAR" property="code" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
    delete from permission
    where id = #{id,jdbcType=VARCHAR}
  </delete>
  <insert id="insert" parameterType="com.task.entity.Permission">
    insert into permission (id, `name`, code)
    values (#{id,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR}, #{code,jdbcType=VARCHAR})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.task.entity.Permission">
    update permission
    set `name` = #{name,jdbcType=VARCHAR},code = #{code,jdbcType=VARCHAR}
    where id = #{id,jdbcType=VARCHAR}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    select id, `name`,code
    from permission
    where id = #{id,jdbcType=VARCHAR}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select id, `name`,code
    from permission
  </select>

  <select id="selectByNameAndCode" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT p.id , p.`code`,p.`name`
    FROM permission p
    LEFT JOIN role_permission rp ON p.id=rp.permission_id
    LEFT JOIN role r ON rp.role_id = r.id
    WHERE r.name = #{arg0} and p.`code`= #{arg1}
  </select>
</mapper>